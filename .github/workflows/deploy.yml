name: Deploy to Azure Functions

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Zip package
        run: zip -r deploy.zip . -x "*.git*"

      - name: Deploy via REST API
        env:
          PUBLISH_PROFILE: ${{ secrets.AZURE_FUNCTIONAPP_PUBLISH_PROFILE }}
        run: |
          USERNAME=$(echo $PUBLISH_PROFILE | grep -oP 'userName="\K[^"]+' | head -1)
          PASSWORD=$(echo $PUBLISH_PROFILE | grep -oP 'userPWD="\K[^"]+' | head -1)
          curl -X POST \
            "https://inbox-marketing-to-trello-app-had5hrcpaha4d3ej.scm.centralus-01.azurewebsites.net/api/publish" \
            -u "$USERNAME:$PASSWORD" \
            -H "Content-Type: application/zip" \
            --data-binary @deploy.zip \
            --fail \
            -v